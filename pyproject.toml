[project]
name = "structured-outputs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.105.2",
    "instructor>=1.15.1",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.2",
]
